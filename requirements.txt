# 替身 (Stand-in) - Python 依赖
#
# 核心功能（对话采集模式）不需要任何依赖。
# 以下依赖仅用于素材解析工具（可选功能）。

# 无强依赖——解析工具使用 Python 标准库实现。
# 如需扩展以下功能，安装对应包：

# 如果需要更精确的中文分词（提升主题分析准确度）：
# jieba>=0.42.1

# 如果需要解析微信 HTML 格式备份：
# beautifulsoup4>=4.12.0
# lxml>=4.9.0

# 如果需要解析 Notion/Obsidian 的数据库格式：
# python-frontmatter>=1.0.0

# 如果需要图片中的文字识别（手写日记等）：
# Pillow>=10.0.0
# pytesseract>=0.3.10  # 需要额外安装 tesseract-ocr

# Python 版本要求：>= 3.10
